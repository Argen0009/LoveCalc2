package mbk.io.lovecalc2

import android.content.Context
import android.widget.Toast

class   Utils {
    fun showToast(context: Context) {
        Toast.makeText(context, "holle", Toast.LENGTH_SHORT).show()
    }
}